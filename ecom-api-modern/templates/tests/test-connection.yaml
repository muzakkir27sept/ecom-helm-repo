apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecom-api-modern.fullname" . }}-test-connection"
  labels:
    {{- include "ecom-api-modern.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecom-api-modern.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
